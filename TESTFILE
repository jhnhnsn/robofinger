test

TESTFILE — live claim test between two machines.

Scratch file. Nothing reads it. Delete when done.
(The bare "test" line above was already here — left it alone.)

Right now `macbook` holds a claim on this path:

    robofinger claim "TESTFILE claim test" 'TESTFILE'

WHAT TO DO ON THE OTHER MACHINE
-------------------------------

1. Confirm the claim arrived:

       robofinger list

   Expect a line under the peer:  claiming robofinger/TESTFILE

2. Ask directly whether editing here would collide:

       robofinger check TESTFILE

   Expect JSON containing CLAIM CONFLICT. Silence means it did not arrive —
   the claim is scoped to project `robofinger`, so the other machine has to
   be inside a checkout with that same repo name for it to fire.

3. Have the agent try to edit this file. The PreToolUse hook runs `check`
   and should surface the conflict before the write, without blocking it.
   Claims are advisory: the warning is the whole product.

4. Then claim it from your side while macbook still holds it. Two live
   claims on one path is the interesting case — nothing prevents it, both
   sides should just see each other.

LOG
---
(append: machine, time, what you saw)

macbook  2026-08-01  claimed TESTFILE, waiting on the other end
